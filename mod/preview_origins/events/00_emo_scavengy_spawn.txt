namespace = emo
event = {
	id = emo.21
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes
	immediate = {
		# "Scavengy" Origin
		every_country = {
			limit = {
				is_country_type = default
				has_origin = origin_scavenger
			}
			log = "useless event"
		}
	}
}
